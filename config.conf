[general]
log_level=info
drone_id = "AS"

[mavlink]
port=14559
host=0.0.0.0

[nmea_objects]
enabled=true

[server]
port=3000
websocket_enabled=true

[features]
photo_capture=true
adsb_handler=true
database_logging=true

[sync]
# Sync data to ground PostgreSQL database
sync_enabled=false
sync_interval_ms=30000
sync_batch_size=50
sync_max_retries=5
sync_cleanup_hours=24

[postgres]
# PostgreSQL connection (can be overridden by environment variables)
# POSTGRES_HOST, POSTGRES_PORT, POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD
host=10.50.0.31
port=5432
database=arduphotologger
user=postgres
# password should be set via POSTGRES_PASSWORD environment variable



